import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Montserrat } from "next/font/google";



export const metadata: Metadata = {
  title: 'Tarek\'s Cafe | Authentic Halal Dining in Tampa',
  description: 'Tarek\'s Cafe offers authentic, delicious halal cuisine in Tampa. Experience our famous Philly cheesesteaks, Cuban sandwiches, and friendly atmosphere at USF.',
  openGraph: {
    "title": "Tarek's Cafe | Authentic Halal Dining in Tampa",
    "description": "Experience Tampa's finest halal cafe at Tarek's. Fresh, friendly, and delicious.",
    "siteName": "Tarek's Cafe",
    "type": "website"
  },
};

const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
});
const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${montserrat.variable} ${inter.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
